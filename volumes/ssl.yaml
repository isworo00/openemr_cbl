apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sslvolume
spec:
  storageClassName: longhorn  # implement longhorn
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi